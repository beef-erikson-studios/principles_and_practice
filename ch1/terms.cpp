#include "PPPheaders.h"

int main() {
    std::cout << "// - Single-line comment.\n";
    std::cout << "function - A reusable container of code.\n";
    std::cout << "output - Anything that gets printed or exported in some way.\n";
    std::cout << "compiler - A program that takes the C++ code and compiles it into an object file.\n";
    std::cout << "error - A program that causes the program to terminate.\n";
    std::cout << "linker - This takes the objects created from the compiler and combines them into an executable.\n";
    std::cout << "command line - Any terminal (bash, command prompt, etc).\n";
    std::cout << "executable - A file that is able to be run the program on the compiled machine.\n\n";

    return 0;
}